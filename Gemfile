source 'https://rubygems.org'

# Deliberately paired with java-service's Maven/Java dependencies in this
# same repo — fixture for tech-stack-advisor's Java+Cucumber pairing
# detection (impact_analyzer.py's java_cucumber_pairings, "same-repo" mode):
# a Java major upgrade can silently break this suite's step definitions,
# with no dependency edge connecting the two at all for the normal
# risk model to walk.
gem 'cucumber', '9.0.0'
